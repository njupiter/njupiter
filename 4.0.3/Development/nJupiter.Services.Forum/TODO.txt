Current:
********
ForumSqlDao:	Type			Description
				----			-----------
				Normal			GetPost with PostType.RootPost should(?) return root post if we specify root post

ForumDao:		Type			Description
				----			-----------
				Enhancement		Filter on attributes on GetCategories
				Enhancement		Secondary sort order (dynamic)
				Enhancement		SearchCriteria on category (on the posts under it?)
				Enhancement		New properties on post, lastAuthor, lastUserIdentity, lastId (phpbb parity)

Arguments:		Type			Description
				----			-----------
				Normal			Check enum params (may be spoofed)
				Normal			Check string length (domain, categoryname)

Database:		Type			Description
				----			-----------
				Enhancement		Save LastID instead of TimeLastPost in FORUM_PostDerivedInformation
				Enhancement		Clustered index on TimePosted
				Enhancement		UserIdentity and all text columns should be NOT NULL to speed up searches
				Known Issue		Sorting only works with default collation in database (problem in multilanguage installations)
				
Old and obsolete:		
*****************
ForumSqlDao:	Type			Description												Reason
				----			-----------												------
				Enhancement		Implement caching										Virtually impossible to cache such volatile 
																						data in an efficient manner

Database:		Type			Description
				----			-----------
				Enhancement		Only have Guid in main tables (post and category),		Just a stupid idea
								and an internalid (int) on other tables		
				Enhancement		Switch from checking with exists in rootdescendantid	Just a stupid idea
								code path and check categoryid not null instead
				Enhancement		TimeLastPost[All|Visible] to speed up sorts 			Obsolete if we implement LastId
				
